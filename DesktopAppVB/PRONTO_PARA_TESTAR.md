# ? PRONTO PARA TESTAR! - Oracle Cloud Configurado

## ?? Build Compilou com Sucesso!

Todas as configurações foram aplicadas corretamente.

---

## ?? TESTE AGORA (3 Passos)

### **Passo 1: Executar Aplicação**

No Visual Studio:
```
Debug ? Start Debugging (F5)
```

ou pressione **F5**

**Esperado:** Mensagem verde "Conexão OK - Oracle Cloud (pontoofflinedb_tp)"

---

### **Passo 2: Abrir Formulário de Empresas**

No menu principal:
1. Clique em **"Empresa"** (ou o menu correspondente)
2. O formulário `frm_empresa` deve abrir

**Esperado:** 
- ? Sem erro ORA-12154
- ? Sem erro ORA-50001
- ? DataGridView vazio (se não houver dados) ou com dados (se já cadastrou)

---

### **Passo 3: Cadastrar Empresa de Teste**

1. Clique no botão **"Novo"**
2. Preencha:
   - CNPJ: `12.345.678/0001-00`
   - Razão Social: `Empresa Teste Oracle Cloud`
   - Nome Fantasia: `Teste Cloud`
   - Email: `teste@cloud.com`
3. Clique em **"Concluir"**

**Esperado:** Mensagem "Empresa cadastrada com sucesso!"

---

## ?? Validar Dados no Oracle Cloud

### Via SQL Developer

```sql
SELECT * FROM tb_empresas;
```

Deve mostrar a empresa cadastrada.

---

## ?? Se Houver Erro

### Erro ORA-12154 (TNS não resolvido)

**Solução Rápida:**

Configure variável de ambiente globalmente:

```powershell
# PowerShell como Administrador
[System.Environment]::SetEnvironmentVariable("TNS_ADMIN", "C:\Users\Guto\Downloads\Wallet_PontoOfflineDB", "Machine")
```

**Depois:**
1. Feche o Visual Studio
2. Reabra o Visual Studio
3. Execute novamente (F5)

---

### Erro ORA-50001 (Connection not open)

**Causa:** Credenciais incorretas ou Wallet inválido

**Verificação:**

1. Abra `App.config` e confirme:
   ```xml
   <add key="OracleUser" value="TESTE_PONTO" />
   <add key="OraclePassword" value="Medeixaempaz2001" />
   <add key="OracleDataSource" value="pontoofflinedb_tp" />
   ```

2. Verifique se o Wallet existe:
   ```powershell
   Test-Path "C:\Users\Guto\Downloads\Wallet_PontoOfflineDB\tnsnames.ora"
   ```
   Deve retornar: `True`

---

### Erro: "Tabela não existe"

**Solução:** Criar tabelas no Oracle Cloud

```sql
-- Execute via SQL Developer

CREATE TABLE tb_empresas (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    cnpj VARCHAR2(18) UNIQUE NOT NULL,
    razao_social VARCHAR2(255) NOT NULL,
    nome_fantasia VARCHAR2(255),
    insc_estadual VARCHAR2(20),
    endereco VARCHAR2(500),
    telefone VARCHAR2(20),
    email VARCHAR2(100),
    logo VARCHAR2(500)
);

CREATE TABLE tb_funcionarios (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    cpf VARCHAR2(14) UNIQUE NOT NULL,
    nome VARCHAR2(255) NOT NULL,
    email VARCHAR2(100),
  cargo VARCHAR2(100),
    ativo NUMBER(1) DEFAULT 1,
    data_cadastro DATE DEFAULT SYSDATE,
 data_admissao DATE,
  data_nasc DATE,
    pis VARCHAR2(15),
    empresa VARCHAR2(255),
    folha VARCHAR2(50),
    horario VARCHAR2(100),
    data_demissao DATE,
    foto VARCHAR2(500)
);

CREATE TABLE tb_registros_ponto (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    funcionario_id NUMBER NOT NULL,
    data DATE NOT NULL,
    hora TIMESTAMP NOT NULL,
    tipo VARCHAR2(16) NOT NULL,
    latitude NUMBER(10,6),
    longitude NUMBER(10,6),
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_funcionario FOREIGN KEY (funcionario_id) 
        REFERENCES tb_funcionarios(id)
);

COMMIT;
```

---

## ? Checklist Final

### Antes de Executar
- [x] ? Build compilou com sucesso
- [x] ? App.config configurado com Oracle Cloud
- [x] ? Module1.vb atualizado para suportar Wallet
- [ ] ? Wallet existe no caminho correto
- [ ] ? Variável TNS_ADMIN configurada (se necessário)

### Ao Executar
- [ ] ? Mensagem "Conexão OK" aparece
- [ ] ? Formulário frm_empresa abre sem erro
- [ ] ? Cadastro de empresa funciona
- [ ] ? Dados aparecem no DataGridView

### Validação
- [ ] ? Consulta SQL mostra dados cadastrados
- [ ] ? CRUD completo funciona (Create, Read, Update, Delete)

---

## ?? O Que Testar

| Funcionalidade | Como Testar | Resultado Esperado |
|----------------|-------------|---------------------|
| **Conexão** | Abrir app (F5) | "Conexão OK - Oracle Cloud" |
| **Listar Empresas** | Abrir frm_empresa | Grid carrega sem erro |
| **Criar Empresa** | Clicar "Novo" ? Preencher ? "Concluir" | "Empresa cadastrada com sucesso!" |
| **Editar Empresa** | Selecionar linha ? Clicar "Editar" | Formulário preenche dados |
| **Excluir Empresa** | Selecionar linha ? Clicar "Excluir" | "Empresa excluída com sucesso!" |

---

## ?? Resultado Esperado

### ? Sucesso Total
```
1. App abre ? "Conexão OK - Oracle Cloud"
2. frm_empresa abre ? Grid vazio ou com dados
3. Cadastro funciona ? Dados salvos no Oracle Cloud
4. Edição funciona ? Dados atualizados
5. Exclusão funciona ? Dados removidos
```

### ?? Sucesso Parcial
```
1. App abre ? "Conexão OK"
2. frm_empresa abre ? Erro "Tabela não existe"
   ? Solução: Criar tabelas (SQL acima)
```

### ? Erro de Conexão
```
1. App abre ? Erro ORA-12154 ou ORA-50001
 ? Solução: Configurar TNS_ADMIN ou verificar credenciais
```

---

## ?? Próxima Ação

**EXECUTE AGORA:**

```
Pressione F5 no Visual Studio
```

e me diga o que aparece! ??

---

**Status:** ? Código Pronto  
**Build:** ? Sucesso  
**Aguardando:** Teste da aplicação

?? **Boa sorte! Tudo deve funcionar agora!**
